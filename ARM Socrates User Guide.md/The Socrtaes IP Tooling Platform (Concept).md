# The Socrates IP Tooling Platform

ARM@R Scorates enables users to browse, configure and build ARM IP including ARM Interconnect IP. It also allows the users to integrate the generted RTL and IP-XACT into ARM based Systems.

The document provides quick overview of the product.