## 3.2 Configured Compoenent API

The Configured Component API creates, configures, and builds configurable components.
To enable the Configured Component API in a script use:

require `ConfiguredCompoenentAPI`

The API *usess the HSL* methods to create the IP Specification

-------
NOTE: To use IP Packages in the sample scripts, associate the appropriate IP packages.

-------

