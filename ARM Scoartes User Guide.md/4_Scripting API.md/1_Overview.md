The script-based API enables tasks to be executed in script form, and output generated. You can
use the **Project Explorer** or the *Command Line Interface (CLI)* to run scripts that you have created.